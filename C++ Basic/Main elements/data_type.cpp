#pragma GCC optimize("O2")
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(0);
    cout.tie(0);
    cin.tie(0);
    cout.setf(ios::fixed);
    cout.precision(10);

    char character; // un caracter
    int numeros_int; // números enteros hasta 10^9
    unsigned int numeros_unsigned_int; // números unicamente positivos hasta 2^32 - 1
    long long numeros_long_long; // números enteros hasta 10^18
    unsigned long long numeros_unsigned_long_long; // números unicamente positivos hasta 2^64 - 1
    float numeros_float; // número real precisión simple (7 dígitos de precisión)
    double numeros_double; // número real precisión doble (15 dígitos de precisión)
    string cadena; // cadena de caracteres
    bool booleano; // solo valores verdadero o falso (true = 1 o false = 0)

    return 0;
}